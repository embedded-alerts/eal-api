# eal-api

Rust Axum, SeaORM, and WebSocket API for embedding-driven alert rules and delivery state.

Initialized through `DEN-1949` as a testable `api` foundation. Product behavior continues through focused pull requests.

```bash
python3 scripts/verify_repo.py
```
